export const rewriteCVWithAI = async (cvText) => {
  // Temporary mock rewrite (until OpenAI integration)
  return `
PROFESSIONAL CV (REWRITTEN)

${cvText}

Improvements Applied:
- Clear professional tone
- Optimized wording
- ATS-friendly formatting
- Strong action verbs
`;
};
